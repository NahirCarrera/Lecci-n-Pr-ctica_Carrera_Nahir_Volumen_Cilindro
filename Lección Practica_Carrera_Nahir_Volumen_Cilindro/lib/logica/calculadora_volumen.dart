class CalculadoraVolumen {
  double calcularVolumen(double radio, double altura) {
    //Fórmula para calcular el volumne de un cilindro V = pi * r^2 * h
    return 3.1416 * radio * radio * altura;
  }
}
